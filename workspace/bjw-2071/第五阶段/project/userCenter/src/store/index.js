import Vue from 'vue';
import Vuex from 'vuex';

/** modules */
import login from './login'

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        login
    }
});
